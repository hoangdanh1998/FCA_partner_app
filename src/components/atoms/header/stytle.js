import { StyleSheet } from 'react-native';
import {PRIMARY_COLOR, BACKGROUND_COLOR} from '../../../constance/constance';

export const styles = StyleSheet.create ({
    icon: {
        fontSize: 27,
        color: "#ffff"
    },
    header: {
        backgroundColor: BACKGROUND_COLOR,
        
    },

    title_header: {
        flexDirection:"row",
        justifyContent:"flex-end",
    }
})
